// Von Klassen-Assistent automatisch erstellte IDispatch-Kapselungsklasse(n). 
/////////////////////////////////////////////////////////////////////////////
// Wrapper-Klasse _ISerPortEvents 

class _ISerPortEvents : public COleDispatchDriver
{
public:
	_ISerPortEvents() {}		// Ruft den Standardkonstruktor für COleDispatchDriver auf
	_ISerPortEvents(LPDISPATCH pDispatch) : COleDispatchDriver(pDispatch) {}
	_ISerPortEvents(const _ISerPortEvents& dispatchSrc) : COleDispatchDriver(dispatchSrc) {}

// Attribute
public:

// Operationen
public:
	// Methode 'OnReceiveChar' wird wegen eines ungültigen Rückgabetyps oder Parametertyps nicht verwendet
	// Methode 'OnBreakDetected' wird wegen eines ungültigen Rückgabetyps oder Parametertyps nicht verwendet
	// Methode 'OnCTSDetected' wird wegen eines ungültigen Rückgabetyps oder Parametertyps nicht verwendet
	// Methode 'OnERRDetected' wird wegen eines ungültigen Rückgabetyps oder Parametertyps nicht verwendet
	// Methode 'OnRINGDetected' wird wegen eines ungültigen Rückgabetyps oder Parametertyps nicht verwendet
	// Methode 'OnRXFLAGDetected' wird wegen eines ungültigen Rückgabetyps oder Parametertyps nicht verwendet
	// Methode 'NotifyTelegramSend' wird wegen eines ungültigen Rückgabetyps oder Parametertyps nicht verwendet
};
